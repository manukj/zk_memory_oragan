{
  "name": "server",
  "version": "1.0.0",
  "description": "Simple Node.js server with store API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup": "mkdir -p build && node scripts/setup.js",
    "postsetup": "cp build/verification_key.json ./"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "express": "^4.18.2",
    "snarkjs": "^0.7.5"
  }
}
